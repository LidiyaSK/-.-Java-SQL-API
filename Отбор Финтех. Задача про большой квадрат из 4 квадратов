//Странная задача. Если я правильно понимаю геометрию, то квадрат можно составить только из 4-х равных квадратов и больше никак.Ну или из большего числа квадратов, тогда могут быть разные.
/*
Ещё с детства Лосяш любит сложные геометрические объекты. Сейчас ему особенно нравятся окружности, тетраэдры и тессеракты, но в детстве он еще даже таких слов не знал, а потому пользовался тем, что было под рукой — квадратами.
Квадрат — самая простая в обращении фигура. Их можно прикладывать друг к другу, и их стороны будут оставаться параллельны или перпендикулярны друг другу. А если очень повезёт, положив рядом несколько квадратов, можно получить новый квадрат!
Давайте поможем Лосяшу определить, можно ли положить друг рядом с другом четыре квадрата со сторонами d1, d2, d3 и d4 так, чтобы каждые два квадрата либо соприкасались по стороне, либо касались углом, либо не пересекались вообще, и при этом чтобы их объединение образовывало бы большой квадрат.
Формат входных данных
В единственной строке ввода через пробел даны четыре числа di(1≤di≤10^9) — стороны квадратов.
Формат выходных данных
Выведите «YES» (без кавычек), если эти четыре квадрата могут образовывать один большой квадрат, и «NO» иначе.
Примеры данных
Пример 1
Ввод
3 3 3 3   
Вывод
YES   
Пример 2
Ввод
3 3 2 3   
Вывод
NO   
*/

//Добавить обработку правильности количества введенных чисел (ввели меньше/больше?)

import java.util.*;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
    Scanner sc = new Scanner(System.in);
  
    int d1 = sc.nextInt();
    int d2 = sc.nextInt();
    int d3 = sc.nextInt();
    int d4 = sc.nextInt();
    sc.close();
     if ((d1>=1)&&(d1<=Math.pow(10,9))&&(d2>=1)&&(d2<=Math.pow(10,9))&&(d3>=1)&&(d3<=Math.pow(10,9))&&(d4>=1)&&(d4<=Math.pow(10,9))){
        if ((d1==d2) && (d2==d3) && (d3==d4))
        System.out.println("YES");
        else
        System.out.println("NO");
    }
       else
       System.out.println("Число d должно соответствовать условию 1>= d >= 10^9");
  
  }
}
